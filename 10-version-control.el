;;****************************************************************
;;****************************************************************
;;****************************************************************
;;****************        VERSION CONTROL         ****************
;;****************************************************************
;;****************************************************************
;;****************************************************************

(defvar sg-on_windows_nt)
(defvar sg-on_darwin)
(defvar sg-on_gnu_linux)
(defvar sg-on_cygwin)

;;________________________________________________________________
;;    git
;;________________________________________________________________

(try-require 'git)

;;________________________________
;;    git & (auto)env
;;________________________________
(setq auto-mode-alist (cons '("\\.gitignore\\'" . sh-mode) auto-mode-alist))
(setq auto-mode-alist (cons '("\\.env\\'" . sh-mode) auto-mode-alist))

;;________________________________________________________________
;;    Show VC differences in dired.
;;________________________________________________________________
(global-diff-hl-mode)     ; enables diff-hl for all buffers
(diff-hl-flydiff-mode)    ; diff-hl marks changes on the fly
(add-hook 'dired-mode-hook 'diff-hl-dired-mode)  ; adds diff-hl for dired mode

;;________________________________________________________________
;;    CVS
;;________________________________________________________________

(setenv "CVS_RSH" "ssh")
